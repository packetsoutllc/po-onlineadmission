import React from 'react';

// This component is no longer used by the new Settings page design.
// It is kept to prevent breaking imports, but its functionality has been
// superseded by the new UI in `SettingsPage.tsx`.
const SchoolsTable: React.FC = () => {
    return null;
};

export default SchoolsTable;